<ul class="list-group">
  <% tags.each do |tag| %>
    <li class="list-group-item">
      <%# link_to tag.name, tag_search_path(tag.name.downcase) %>
      <%= link_to "#{tag[:name]} (#{tag[:total_posts]})", tag_search_path(tag[:name].downcase) %>
    </li>
  <% end%>
</ul>