<div class="container">
  <div class="row"> 
    <div class="col-md-12">
      <div class="row">
        <% @posts.each do |post|  %>
          <%= render partial: 'post', locals: {post: post}  %>
        <% end%>
      </div>
    </div>
  </div>
  <%= paginate @posts %>
</div>
