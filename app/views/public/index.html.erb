<div class="container">
  <div class="row"> 
    <div class="col-md-9">
      <div class="row">
        <% @posts.each do |post|  %>
          <%= render partial: 'post', locals: {post: post}  %>
        <% end%>
      </div>
    </div>
  </div>
  <%= paginate @posts %>
</div>
